version: 0.2

phases:
  install:
    commands:
      - echo installing dependencies
      # no dependencies needed for static HTML

  build:
    commands:
      - echo Build project.
      - ls -la # lists files for verification

artifacts:
  files:
    - '**/*'  # includes all project files in the output
